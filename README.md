# Binary Search in C++

Simple, clean implementation of the binary search algorithm.

## Features
- Iterative implementation (O(1) space)
- Overflow-safe midpoint calculation
- Multiple test cases included

## Compile & Run

```bash
g++ -std=c++17 -o binary_search binary_search.cpp
./binary_search# c-programming
simple c+programming
